{
  "name": "@bricktowers/identity-api",
  "version": "0.1.0",
  "author": "Brick Towers",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "webpack --config webpack.config.js",
    "serve": "node --enable-source-maps dist/bundle.js",
    "buildAndRestart": "yarn build && yarn serve",
    "test": "vitest run",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@bricktowers/rwa-contract": "*",
    "buffer": "^6.0.3"
  },
  "devDependencies": {
    "eslint": "^8.52.0",
    "eslint-config-standard-with-typescript": "^39.1.1",
    "vitest": "^1.6.1",
    "ts-loader": "^9.5.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2",
    "webpack": "^5.97.1",
    "webpack-cli": "^6.0.1",
    "webpack-node-externals": "^3.0.0"
  }
}
